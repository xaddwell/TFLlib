# OpenSource Todo List

- Use uv to re-construct the Env and export the env toml file.
- add a bash script to download the dataset
- add a bash script to download the pretrained model
- run the code locally and test it on different datasets
- 